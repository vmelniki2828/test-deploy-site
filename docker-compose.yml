version: '3'

networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "3000:3000"  # Прокидываем порт 3000 наружу
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend:
    build:
      context: ./CatChatBackReborn
    environment:
      - MONGO_URI=mongodb://mongo:27017/chat
    ports:
      - "8000:8000"  # Бэкенд работает на 8000 порту
    networks:
      - dev
    depends_on:
      - mongo

  frontend:
    build:
      context: ./CatChat
    expose:
      - "80"  # Фронтенд работает на порту 80 внутри сети
    networks:
      - dev

  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db
    networks:
      - dev

volumes:
  mongo-data: